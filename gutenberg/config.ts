import * as config from '../config'

export const about = config.about
export const description = config.description
export const domainName = config.domainName
export const keywords = config.keywords
export const owner = config.owner
export const title = config.title
export const headline = config.headline
export const sections = config.sections
export const subtitle = config.subtitle
export const links = config.links
// Analytics
export const gaTrackingId = config.gaTrackingId || null
